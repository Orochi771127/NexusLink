# Formal Evolution Owner Lock R2

## Locked on 2026-08-13

The Owner approved the existing R2 Stage 2 and Stage 3 visual designs for ten
lineages. Their per-character metadata now uses `artStatus: owner-approved` and
`humanApproved: true` for the two later forms.

- `greyshade-cat`
- `sprigfawn`
- `crystalfin-seahorse`
- `blazetail-kit`
- `starstripe-cub`
- `thunder-pup`
- `wavecub`
- `starflame-phoenix`
- `star-foal`
- `goldenspark-wyrm`

The exact approved files and SHA-256 fingerprints are sealed in
`assets/reference/formal-evolution-r1/lineage-board-owner-lock-r2.json`.

## Deliberate exclusion

`auriowl` is not included in this approval lock. The Owner replaced the prior
owl visual direction with a supplied Golden War Hawk Stage 1 identity. The R2
owl board remains preserved as non-authoritative historical review evidence;
the technical ID remains `auriowl` so existing saves, unlocks, relationship
state and Growth evidence do not split into a second companion.

## What this lock does not mean

- It does not make any later form `runtime-ready`.
- It does not create a 512x512 transparent production master.
- It does not provide Expedition or Orbit animation assets.
- It does not implement Growth G4 invitation, willingness or stage advance.
- It does not enable runtime form swapping.

Each approved lineage still needs a species-specific transparent master,
front/side/back proportion freeze, eight-direction animation QC and a separate
runtime promotion gate. All current Stage 1 runtime art remains the safe
same-owner fallback.
